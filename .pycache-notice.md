# Placeholder - Cache files should be in .gitignore, not committed

This directory should not contain __pycache__ files.
If you see .pyc files here, run:
  git rm -r --cached Day_*/__pycache__
  git commit -m "Remove Python cache files"

The .gitignore file at repository root prevents these from being committed.
